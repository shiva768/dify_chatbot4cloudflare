{
  "name": "slack-dify-bot",
  "version": "1.0.0",
  "description": "Slack bot with Dify integration using Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "test": "jest",
    "logs": "wrangler tail",
    "kv:list": "wrangler kv:key list --binding=THREAD_MAP"
  },
  "dependencies": {
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "jest": "^29.0.0"
  }
}
