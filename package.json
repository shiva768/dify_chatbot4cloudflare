{
  "name": "slack-dify-bot",
  "version": "1.0.0",
  "description": "Slack bot with Dify integration using Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "jest"
  },
  "dependencies": {
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "jest": "^29.0.0"
  }
}
